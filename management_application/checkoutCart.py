

class UserCart(User):
    def __init__(self),
        self.items_in_cart = []

    def add_to_cart(item):
        try:
            self.items_in_cart.append(item)
        except:
            print("item couldn't be added to cart")
